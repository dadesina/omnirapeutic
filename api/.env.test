# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database Configuration
# CRITICAL SECURITY: NEVER use production database for tests!
#
# CI/CD: GitHub Actions overrides this with ephemeral PostgreSQL container
# Local: Use docker-compose or local PostgreSQL test database
#
# Default local test database (update for your local setup):
DATABASE_URL="postgresql://test_user:test_password@localhost:5432/omnirapeutic_test"

# JWT Configuration
JWT_SECRET=test-secret-key-for-testing-only-do-not-use-in-production
JWT_EXPIRES_IN=1h

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Rate Limiting (relaxed for tests)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
AUTH_RATE_LIMIT_MAX_REQUESTS=50
